{
  "name": "@quest/analytics",
  "version": "0.0.0",
  "private": true,
  "description": "Product analytics contracts: governed event naming, consent-aware envelope and the sink port. No vendor SDK; instrumentation is wired in later phases.",
  "type": "commonjs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.5.4"
  }
}
